name: Angular CI/CD Pipeline for GitHub

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    strategy:
      matrix:
        node-version: ['22.x']

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - name: Environment output
        run: echo "Building for '${{ inputs.environment }}' environment"

      - name: Install dependencies
        run: npm ci

      - name: Package app
        run: npm run build

  code-style:
    needs: build
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    strategy:
      matrix:
        node-version: ['22.x']
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - name: Environment output
        run: echo "Building for '${{ inputs.environment }}' environment"

      - name: Install dependencies
        run: npm ci

      - name: Code style check
        run: npm run code-style-check
  test-unit:
    needs: build
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    strategy:
      matrix:
        node-version: ['22.x']
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - name: Environment output
        run: echo "Building for '${{ inputs.environment }}' environment"

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm run test -- --watch=false --browsers=ChromeHeadlessCI
#   build-and-test:
#     runs-on: ubuntu-latest  # You can specify a different OS if necessary

#     steps:
#       # Step 1: Checkout the code
#       - name: Checkout code
#         uses: actions/checkout@v2

#       # Step 2: Set up Node.js (using the latest LTS version)
#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 'lts'

#       # Step 3: Install dependencies
#       - name: Install dependencies
#         run: |
#           npm install

#       # Step 4: Run code style check (Prettier)
#       - name: Check code style
#         run: |
#           npm run code-style-check

#       # Step 5: Run unit tests
#       - name: Run tests
#         run: |
#           npm run test -- --watch=false --browsers=ChromeHeadlessCI

#       # Step 6: Build the application
#       - name: Build the application
#         run: |
#           npm run build

# Optionally, you can add a deploy job here, depending on your deployment setup.
